import tile1JSON from '../tilemaps/tile1.json'

// This class loads all tilemaps from json files
// It manages coins, dangerous o

export default class tilemapManager {
    
    constructor(scene, scale = 1) {
        this.scene = scene;
        this.mapScale = scale;
    }

    create()
    {

    }
}